import React from 'react';
import {Link} from 'react-router-dom';

function NavRecipeMenuItem(props) {
    // <Link to={`/recipe/${props.id}`}>{props.name}</Link>
}

export default NavRecipeMenuItem;
